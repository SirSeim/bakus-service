name: CI

# TODO: do we need this
#env:
#  DB_NAME: dashboard_api
#  DB_USER: root
#  DB_PASSWORD: root

on:
  push:
    branches: [main]
  pull_request:
  release:
    types: [released]

jobs:
  linter:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Lint Code Base
        uses: github/super-linter/slim@v4
        env:
          DEFAULT_BRANCH: main
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          VALIDATE_ALL_CODEBASE: true
          VALIDATE_PYTHON_BLACK: true
          VALIDATE_PYTHON_ISORT: true
          LINTER_RULES_PATH: /
          PYTHON_BLACK_CONFIG_FILE: pyproject.toml
          PYTHON_ISORT_CONFIG_FILE: pyproject.toml

  pytest:
    runs-on: [ubuntu-latest]
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10"]

    steps:
    - uses: actions/checkout@v3

# TODO: If we need this, setup postgres equivalent
#    - name: Set up MySQL
#      run: |
#        sudo systemctl start mysql
#        test -e /var/lib/mysql/${{ env.DB_NAME }} || mysqladmin --user='${{ env.DB_USER }}' --password='${{ env.DB_PASSWORD }}' create '${{ env.DB_NAME }}'

    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v3
      with:
        python-version: ${{ matrix.python-version }}
        cache: pip

    - name: Ensure latest pip
      run: |
        python -m pip install --upgrade pip

    - name: Install Python dependencies
      run: |
        pip install -r requirements-dev.txt

    - name: Test with pytest
      run: |
        pytest -n auto --cov-report=xml --cov=addition

    - name: Coveralls
      uses: coverallsapp/github-action@master
      with:
        github-token: ${{ secrets.GITHUB_TOKEN }}
        path-to-lcov: coverage.xml
